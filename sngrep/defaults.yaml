# -*- coding: utf-8 -*-
# vim: ft=yaml
---
sngrep:
  pkg:
    name: sngrep
  rootgroup: root
  config: '/etc/sngrep'
  service:
    name: sngrep
  subcomponent:
    config: '/etc/sngrep-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
